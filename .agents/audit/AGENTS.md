# .agents/audit

This directory is reserved for repository-level audit metadata only.

- Do not store audit reports here.
- Store audit reports in `/tmp/agents-artifacts/`.
- This directory must contain only this `AGENTS.md`.
